### Summary

<!-- What this change does and why, in a paragraph. Note anything the
     library gains or loses beyond the stated scope. -->

### Changes

<!-- One self-contained bullet per change, past tense: what was done, and the
     reason or consequence that makes it worth doing. Follow the conventions
     in DESIGN.md (module placement, leg ordering, docstring and dash style);
     new behavior comes with tests. -->

### Validation

<!-- Commands run and their results:
     - `pytest` passed (N passed)
     - `mypy` passed
     - `pre-commit run --all-files` passed with no files modified
     plus anything specific to this change. -->
